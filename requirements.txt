# GUI dependency (Tkinter - usually built into Python, but added for completeness)
tk

# Nmap must be installed on the system (not via pip)
# Download from https://nmap.org/download.html or install via your package manager
